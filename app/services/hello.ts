/**
 * ⚠️ This file is a dummy service function.
 * You can store your business logic here and separate them
 * into different files according to your actual project needs.
 */

export async function greetUser(name: string) {
  return `Hello! ${name}`;
}
